Project: Garmin-Fitbit "Shadow Sync"Goal: A Dockerized Python service that pulls Fitbit Intraday data (HR/Steps) and injects it into Garmin Connect as "Monitoring" FIT files, impersonating a specific Garmin Forerunner to trigger Physio TrueUp and Daily Suggested Workouts.1. Technical ArchitectureSource: Fitbit Web API (Intraday Heart Rate & Activity Time Series).Processing Engine: Python 3.11+ using the Garmin FIT SDK (to encode binary .fit files).Target: Garmin Connect "Sync" Endpoints via the garth library.Deployment: Docker container with a cron or sleep loop.2. Phase 1: Authentication & Identity DiscoveryThe agent must first establish the "DNA" of your Garmin device to ensure the servers accept the data as "native."Requirements:Fitbit side: OAuth2 client ID/Secret with heartrate and activity scopes. (User must provide a token.json).Garmin side: Username/Password.Identity Extraction:The script must include a utility to parse a "template" .fit file from the user's Forerunner.Logic: Use fitparse to extract manufacturer, product, and serial_number from the file_id message of a real activity.3. Phase 2: The "Monitoring" FIT File GeneratorThis is the most critical part. Garmin Coach looks at Monitoring files (Type 9), not just Activities.File Specification:Header: Must set file_id type to 9 (Monitoring).Messages: * device_info: Must contain the spoofed Product ID and Serial Number.monitoring: High-frequency messages containing timestamp, heart_rate, and cycles (cumulative steps).Constraints: Garmin expects Monitoring files to cover discrete chunks of time (e.g., 15-minute or 1-hour intervals).Instruction for AI: "Implement a FIT encoder using the official Garmin FIT SDK (Python wrapper) that maps Fitbit Intraday JSON responses to monitoring messages."4. Phase 3: The Sync Logic (Garth Integration)Standard "Activity Upload" endpoints often ignore 24/7 metrics. We must use the Sync pathway.Implementation Details:Use the garth library for session persistence.Endpoint: Instead of .upload_activity(), the agent should explore the client.upload() method which targets the binary sync service.Conflict Resolution: The script should check for existing data in Garmin to avoid overwriting "real" Forerunner data with "Fitbit" data for the same time window.5. Phase 4: Project Structure (For the AI)The AI should generate the following file structure:Plaintext/shadow-sync
├── Dockerfile
├── main.py                # Main loop & Orchestration
├── fit_engine.py          # FIT SDK logic (The "Spoofer")
├── fitbit_client.py       # API calls to Fitbit
├── garmin_client.py       # Garth wrapper for binary uploads
├── identity_grabber.py    # Utility to extract ID from local .fit files
├── requirements.txt
└── config.yaml.example    # Garmin IDs, Fitbit Secrets
6. Implementation Prompt for Gemini/ClaudeCopy and paste this to start the build:"Act as a Python engineer specialized in Wearable APIs. Build a tool called 'Shadow Sync.'Use the garth library for Garmin authentication and fitbit for Fitbit API.Primary task: Fetch Fitbit Intraday HR/Steps data for the last 4 hours.Secondary task: Generate a binary .fit file of type 'Monitoring' (Type 9). You MUST allow me to input a manufacturer, product_id, and serial_number so the file impersonates my Forerunner.Tertiary task: Upload this file to Garmin using the binary sync endpoint.Requirements: Use Python 3.11, provide a Dockerfile, and ensure it handles OAuth2 token refreshing for Fitbit automatically.Focus on the fit_engine.py: It must use the Garmin FIT SDK to ensure the CRC and header are valid, otherwise Garmin will reject the 'fake' device data."7. Known Risks & MitigationsRiskMitigationGarmin ignores dataEnsure the device_info message in the FIT file matches the "Primary Wearable" ID in your Garmin Connect account.Account BanKeep the sync frequency reasonable (e.g., once every 2 hours) to avoid triggering Garmin’s anti-spam.Inaccurate Body BatteryNote that Body Battery is often calculated on-device. Injected data may only affect "Training Load" and "Suggested Workouts," not the Body Battery graph.jk
